{
  "name": "nexaride",
  "version": "1.0.0", 
  "description": "NexaRide - Servizio Taxi Premium",
  "scripts": {
    "build": "echo 'Static site ready' && mkdir -p dist && cp -r public/* dist/ && cp index.html dist/",
    "deploy": "echo 'Deploy ready'"
  }
}
